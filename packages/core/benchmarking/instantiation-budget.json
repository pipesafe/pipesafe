{
  "maxInstantiations": 975000,
  "lastMeasured": 894464,
  "note": "Whole-project type instantiations for tsconfig.benchmark.json (src + examples + benchmarking, dist built). Deterministic for a pinned TypeScript version. History: 850,648 after the nested-validation layer; 919,425 after review round 1 (operand widenings, array walk); 894,464 after review round 2 (readonly operand positions in the registry replaced a DeepMutable re-check that measured +280k/2x check time; forgiving unknown operators; comparable $min/$max). Still below pre-standardization main's 988k, check ~3.7s vs 4.9s. If this check fails: investigate the regression first (PR #99's depth-viewer attributes per-symbol cost); raise the budget only as a deliberate, reviewed decision \u2014 update lastMeasured and note the cause here."
}
