{
  "maxInstantiations": 1115000,
  "lastMeasured": 1079143,
  "note": "Whole-project type instantiations for tsconfig.benchmark.json (src + examples + benchmarking, dist built). Deterministic for a pinned TypeScript version. History: 850,648 nested-validation layer; 919,425 round-1 correctness (operand widenings, array walk); 894,464 round-2 (registry-readonly replaced a DeepMutable re-check measured at +280k/2x); 972,749 round-3 (readonly dependent-inference patterns — fixes silent field drops for $cond/$ifNull/$arrayElemAt/$concatArrays under const inference — plus non-$ string comparables and new regression pins); 1,054,118 PR-review round (deliberate +81k, attributed: ~34k new regression pins incl. generic-schema helper fixtures, ~47k unknown-operator/accumulator rejection via explicit Unimplemented allow-lists + the schema-free fast-accept that makes expression values compile in generic-schema helpers, ~1k registry-derived category key sets). 1,079,143 dotted-$lookup-as nesting (+ registry returns-omission encoding; the dotted path reuses $set's FlattenDotSet+ApplySetUpdates — keeping the cheap Omit form for FLAT as-keys avoided ~+42k more). If this check fails: investigate first (PR #99's depth-viewer attributes per-symbol cost); raise the budget only as a deliberate, reviewed decision — update lastMeasured and note the cause here."
}
