---
"@pipesafe/core": minor
---

Add type-safe `$out` stage support for `{ db, coll }` and time-series output collections, with `timeField` constrained to `Date` field references and `metaField` constrained to non-`_id` field references
